{
  "name": "jshwwedding-qa-firebase-tests",
  "version": "1.0.0",
  "private": true,
  "description": "축하 화환(wreaths) Firestore 통합 테스트 — 에뮬레이터 (작성순 정렬 · 보안 규칙)",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-jshw --only firestore \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.2",
    "firebase-tools": "^13.23.1"
  }
}
